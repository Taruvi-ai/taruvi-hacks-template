name = "taruvi-webhook-handler"
main = "worker.js"
compatibility_date = "2024-01-15"
account_id = "f41dc829ca090ecf54db53d03655602a"

# Environment variables (set via Cloudflare dashboard or wrangler secret)
# GITHUB_TOKEN - GitHub PAT with repo and workflow permissions
# WEBHOOK_SECRET - Random string for webhook signature validation
