# QUICKSTART

1. Set Langfuse keys in `.env`.
2. Set Taruvi values in `.env`.
3. Set `X-App-Slug` in `.codex/config.toml`.
4. Enable PreToolUse/PostToolUse/Stop hooks in `.codex/config.toml`.
5. Run one Codex tool action and verify events in Langfuse filtered by `app_slug`.
