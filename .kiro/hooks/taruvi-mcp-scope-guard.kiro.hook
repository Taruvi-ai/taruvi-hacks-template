{
  "name": "Taruvi MCP Scope Guard",
  "version": "1.1.0",
  "description": "Before any Taruvi MCP tool call, confirm which Taruvi server the call will hit and that the live session has actually bound the current config. Kiro merges MCP config user < workspace and binds servers at session start, so a freshly cloned template can silently operate against another project's tenant. Stays silent once the workspace connection is confirmed.",
  "when": {
    "type": "preToolUse",
    "toolTypes": [
      "(?i).*taruvi.*",
      "(?i)mcp_.*_(get_datatable_schema|create_update_schema|datatable_data|datatable_edges|delete_datatable|execute_raw_sql|manage_policies|manage_function|execute_function|manage_query|execute_query|manage_roles|manage_role_assignments|list_users|create_user|update_user|user_attributes_schema|manage_storage|list_secrets|get_secret|create_update_secret|manage_secret_types|manage_tags|get_ai_docs)"
    ]
  },
  "then": {
    "type": "runCommand",
    "command": "python3 scripts/mcp_scope.py --quiet",
    "timeout": 20
  }
}
